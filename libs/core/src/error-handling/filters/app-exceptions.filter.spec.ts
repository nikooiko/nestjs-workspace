import { AppExceptionsFilter } from './app-exceptions.filter';

describe('AppExceptionsFilter', () => {
  it('should be defined', () => {
    expect(new AppExceptionsFilter({} as any)).toBeDefined();
  });
});
